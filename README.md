# Demo Mongo with Express.js

## How to run

1. `yarn` or `npm install` for install dependencies
2. `yarn dev` or `npm run dev` for run dev mode
3. Open [http://localhost:3000](http://localhost:3000)
